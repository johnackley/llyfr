
class SeriesListResponse {
  constructor(data) {
    // console.log('series-work data:', data);
    // this.series_works = data.series_works[0].series.map(x => new WorkSeries(x));
    // console.log('parsed series-work:', this);
  }
}

module.exports = { SeriesListResponse };
